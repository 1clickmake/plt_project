<?php $str = '[{"path":"\/uploads\/quotes_extra\/ext_1787743446_2302.pdf","original_name":"영구크린 캐드도면-모형.pdf"}]'; $extraFiles = $str; $decodeLimit = 5; while (is_string($extraFiles) && $decodeLimit > 0) { $decoded = json_decode($extraFiles, true); if ($decoded === null && json_last_error() !== JSON_ERROR_NONE) { break; } $extraFiles = $decoded; $decodeLimit--; } if (!is_array($extraFiles)) { $extraFiles = []; } echo json_encode($extraFiles, JSON_UNESCAPED_UNICODE); ?>
